# 📱 دليل تشغيل التطبيق على الموبايل - Sakina Quran App

## ✅ التحقق من عمل جميع الميزات

هذا الدليل يساعدك في التأكد من أن **كل ميزة تعمل بشكل صحيح** على تطبيق Android (APK).

---

## 🔐 1. تسجيل الدخول (Authentication)

### **الميزات المدعومة:**
- ✅ تسجيل بالبريد الإلكتروني وكلمة المرور
- ✅ تسجيل عبر Google
- ✅ استعادة كلمة المرور عبر OTP
- ✅ حفظ الجلسة تلقائياً

### **كيفية الاختبار:**
```bash
1. افتح التطبيق
2. اضغط "تسجيل الدخول"
3. أدخل بريدك وكلمة المرور
4. يجب أن يتم تسجيل الدخول بنجاح
5. أغلق التطبيق وافتحه مرة أخرى → يجب أن تبقى مسجلاً
```

### **الملفات المسؤولة:**
- `src/components/AuthGate.tsx` - نظام المصادقة
- `src/lib/firebase.ts` - إعدادات Firebase
- `.env.local` - مفاتيح API

### **استكشاف الأخطاء:**
```
❌ مشكلة: لا يمكن تسجيل الدخول
✅ الحل: تأكد من:
   - اتصال الإنترنت
   - Firebase Config صحيح في src/lib/firebase.ts
   - حساب Firebase مفعل (Email/Password + Google)
```

---

## 🔔 2. نظام الإشعارات (Notifications)

### **أنواع الإشعارات:**

#### **أ. إشعارات الأذان (Prayer Notifications)**
```typescript
// الملف: src/lib/prayerNotifications.ts
```
- ✅ 60 إشعار مجدول (يوم كامل)
- ✅ صوت الأذان يعمل في الخلفية
- ✅ اختيار المؤذن (3 مؤذنين)
- ✅ يعمل حتى مع إغلاق التطبيق

**الاختبار:**
```bash
1. اذهب إلى قسم "الصلاة"
2. اضغط على أي صلاة → "إعدادات"
3. فعّل "تنبيه الموعد" + "صوت الأذان"
4. اختر المؤذن
5. انتظر وقت الصلاة → يجب سماع الأذان
```

#### **ب. الإشعارات الذكية (Smart Notifications)**
```typescript
// الملف: src/lib/smartNotifications.ts
```
- ✅ تذكير يومي بالورد
- ✅ تذكير بالأذكار المنسية (6 أنواع):
  - أذكار الصباح (5-10 صباحاً)
  - أذكار المساء (3-7 مساءً)
  - أذكار النوم (9-12 مساءً)
  - أذكار الاستيقاظ (5-9 صباحاً)
  - أذكار الخروج من المنزل
  - أذكار دخول المنزل
- ✅ تذكير سورة الكهف يوم الجمعة
- ✅ رسائل مخصصة حسب نشاط المستخدم

**الاختبار:**
```bash
1. اذهب إلى الإعدادات → الإشعارات
2. فعّل "الإشعارات الذكية"
3. فعّل "تذكير الأذكار"
4. انتظر الوقت المحدد → يجب ظهور إشعار
```

#### **ج. Push Notifications**
```typescript
// الملف: src/lib/pushNotifications.ts
```
- ✅ إشعارات من Firebase Cloud Messaging
- ✅ تعمل حتى مع إغلاق التطبيق

---

## 🕌 3. مواقيت الصلاة والأذان

### **كيف يعمل:**
```typescript
1. تحديد الموقع عبر GPS
2. جلب المواقيت من AlAdhan API
3. تخزينها في Cache محلي
4. جدولة 60 إشعار مقدماً
5. تشغيل الصوت عند الوقت المحدد
```

### **الملفات الرئيسية:**
- `src/components/PrayerTimes.tsx` - واجهة العرض
- `src/lib/prayerNotifications.ts` - جدولة الإشعارات
- `src/lib/prayerCalendar.ts` - حساب المواقيت

### **التأكد من العمل:**
```bash
✅ الخطوة 1: السماح بالموقع
   - عند أول تشغيل، اسمح بالوصول للموقع
   
✅ الخطوة 2: التحقق من المواقيت
   - اذهب لقسم "الصلاة"
   - يجب عرض مواقيت اليوم بدقة
   
✅ الخطوة 3: اختبار الأذان
   - فعّل إشعارات صلاة معينة
   - غيّر وقت الهاتف لوقت الصلاة
   - يجب سماع الأذان
```

### **استكشاف الأخطاء:**
```
❌ الأذان لا يعمل في الخلفية
✅ الحل:
   1. اذهب لإعدادات Android → البطارية
   2. ابحث عن التطبيق
   3. فعّل "السماح بالنشاط في الخلفية"
   4. عطّل "تحسين البطارية" للتطبيق

❌ المواقيت غير دقيقة
✅ الحل:
   1. تأكد من تفعيل GPS
   2. تحديث الصفحة لسحب المواقيت الجديدة
   3. تحقق من المدينة الصحيحة
```

---

## 📍 4. الموقع الجغرافي (GPS)

### **الصلاحيات المطلوبة:**
```xml
<!-- AndroidManifest.xml -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```

### **الاستخدامات:**
- ✅ حساب مواقيت الصلاة الدقيقة
- ✅ اتجاه القبلة
- ✅ المسافة للكعبة

### **الاختبار:**
```bash
1. اذهب لقسم "القبلة"
2. يجب طلب إذن الموقع
3. بعد السماح، يجب عرض اتجاه القبلة
```

---

## 🎧 5. المكتبة الصوتية

### **الميزات:**
- ✅ تشغيل السور كاملة
- ✅ مشغل متكامل (تشغيل، إيقاف، تخطي)
- ✅ يعمل في الخلفية
- ✅ Media Session API (تحكم من شاشة القفل)

### **الملفات:**
- `src/components/AudioLibrary.tsx`
- `src/lib/mediaSession.ts`

### **الاختبار:**
```bash
1. اذهب لقسم "المكتبة الصوتية"
2. اختر سورة وقارئ
3. اضغط تشغيل
4. اقفل الشاشة → يجب استمرار التشغيل
5. تحكم من شاشة القفل → يجب العمل
```

---

## 📿 6. الأذكار والعداد

### **الميزات:**
- ✅ أذكار الصباح والمساء
- ✅ عدّاد إلكتروني
- ✅ منح نقاط تلقائي
- ✅ نسخ الذكر

### **الملفات:**
- `src/components/AthkarLibrary.tsx`
- `src/lib/points.ts`

### **الاختبار:**
```bash
1. اذهب لقسم "الأذكار"
2. اختر ذكر واضغط عليه
3. يجب زيادة العداد
4. يجب منح نقاط (تحقق من النقاط في البروفايل)
```

---

## 🏆 7. نظام النقاط

### **كيف يعمل:**
```typescript
// الملف: src/lib/points.ts

قراءة القرآن:     1 نقطة لكل آية
الاستماع:         1 نقطة لكل 30 ثانية
الأذكار:          1 نقطة لكل ذكر
صناعة الفيديو:    10 نقاط لكل فيديو
إكمال السورة:     50 نقطة مكافأة

حدود يومية:
- قراءة:  500 نقطة/يوم
- استماع: 300 نقطة/يوم
- أذكار:  200 نقطة/يوم
```

### **الاختبار:**
```bash
1. اقرأ بعض الآيات → تحقق من زيادة النقاط
2. استمع لسورة → تحقق من النقاط
3. اقرأ أذكار → تحقق من النقاط
4. اذهب للوحة المتصدرين → يجب ظهور اسمك
```

---

## 🎬 8. استوديو الفيديو

### **الميزات:**
- ✅ تصميم فيديوهات عمودية (9:16)
- ✅ خلفيات من Pexels
- ✅ موجة صوتية مرئية
- ✅ رسوم متحركة
- ✅ تصدير بجودة عالية

### **الملفات:**
- `src/components/VideoPreview.tsx`
- `src/components/RenderModal.tsx`
- `src/app/api/render/route.ts`

### **الاختبار:**
```bash
1. اذهب لقسم "الفيديو"
2. اختر سورة وآيات
3. اختر خلفية وتأثيرات
4. اضغط "تصدير الفيديو"
5. انتظر الريندر → يجب تحميل الفيديو
```

### **ملاحظة مهمة:**
```
⚠️ الريندر يحتاج سيرفر Next.js يعمل
⚠️ في APK، يجب الاتصال بـ Vercel مباشرة
✅ تم ضبط هذا تلقائياً في AuthGate.tsx
```

---

## 🤖 9. AI Chatbot

### **الميزات:**
- ✅ أسئلة إسلامية
- ✅ تفسير الآيات
- ✅ وضع الاختبارات
- ✅ إنشاء خطط قرآنية

### **الملفات:**
- `src/components/ChatBot.tsx`
- `src/app/api/chat/route.ts`

### **الاختبار:**
```bash
1. اضغط زر ChatBot العائم
2. اسأل سؤال إسلامي
3. يجب الحصول على إجابة من Gemini AI
```

### **متطلبات:**
```
✅ GEMINI_API_KEY في .env.local
✅ اتصال إنترنت
```

---

## 🔋 10. العمل في الخلفية (Background Tasks)

### **ما يعمل في الخلفية:**
```typescript
✅ إشعارات الأذان (LocalNotifications)
✅ الإشعارات الذكية (Smart Notifications)
✅ Push Notifications
✅ تشغيل الصوت (Media Session)
```

### **إعدادات Android المطلوبة:**
```bash
1. إعدادات → التطبيقات → القرآن الكريم
2. البطارية → تعطيل "تحسين البطارية"
3. الأذونات → السماح بـ:
   - الإشعارات
   - الموقع (دائماً)
   - التخزين
   - الميكروفون (للتسجيل)
```

---

## 📦 11. بناء APK للإنتاج (Release Build)

### **الخطوات:**
```bash
# 1. بناء Next.js
npm run build

# 2. مزامنة Capacitor
npx cap sync android

# 3. إنشاء Keystore (مرة واحدة فقط)
cd android
keytool -genkey -v -keystore sakina-release.keystore -alias sakina -keyalg RSA -keysize 2048 -validity 10000

# 4. إضافة Keystore لـ gradle
# عدّل android/app/build.gradle وأضف:
android {
    signingConfigs {
        release {
            storeFile file("../sakina-release.keystore")
            storePassword "YOUR_PASSWORD"
            keyAlias "sakina"
            keyPassword "YOUR_PASSWORD"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

# 5. بناء Release APK
./gradlew assembleRelease

# الناتج:
# android/app/build/outputs/apk/release/app-release.apk
```

---

## 🐛 12. استكشاف الأخطاء الشائعة

### **مشكلة: التطبيق لا يفتح**
```bash
✅ الحل:
1. امسح بيانات التطبيق
2. أعد التثبيت
3. تحقق من Logcat للأخطاء:
   adb logcat | grep -i "quran"
```

### **مشكلة: الأذان لا يعمل**
```bash
✅ الحل:
1. تحقق من صلاحيات الإشعارات
2. تعطيل تحسين البطارية
3. تأكد من تفعيل "صوت الأذان" في الإعدادات
4. جرب إعادة جدولة الإشعارات:
   - اذهب لإعدادات الصلاة
   - عطّل ثم فعّل الإشعارات
```

### **مشكلة: تسجيل الدخول لا يعمل**
```bash
✅ الحل:
1. تحقق من اتصال الإنترنت
2. تأكد من Firebase Config
3. تحقق من Console للأخطاء
4. جرب تسجيل الدخول عبر Google
```

### **مشكلة: الفيديوهات لا تُصدَّر**
```bash
✅ الحل:
1. تأكد من اتصال الإنترنت
2. تحقق من PEXELS_API_KEY
3. راجع Logs في Vercel Dashboard
4. جرب فيديو أقصر (آية واحدة)
```

---

## 📊 13. Checklist نهائي قبل النشر

### **قبل رفع APK:**
```
□ تسجيل الدخول يعمل (Email + Google)
□ الإشعارات تعمل (أذان + ذكية)
□ الأذان يشتغل في الخلفية
□ GPS يعمل (مواقيت + قبلة)
□ المكتبة الصوتية تعمل في الخلفية
□ الأذكار والعداد يعملان
□ نظام النقاط يعمل
□ الفيديوهات تُصدَّر
✓ ChatBot يجيب على الأسئلة
✓ لا توجد أخطاء في Logcat
✓ الحجم مقبول (<100MB)
✓ يعمل على Android 5.1+
```

### **أوامر مفيدة للاختبار:**
```bash
# مراقبة Logs
adb logcat | grep -E "Notifications|Prayer|Auth"

# فحص Permissions
adb shell dumpsys package quran1mu.vercel.app | grep permission

# اختبار Background
adb shell am force-stop quran1mu.vercel.app
# انتظر وقت الصلاة → يجب سماع الأذان
```

---

## 🎯 14. نصائح للأداء الأمثل

### **لتحسين الأداء:**
```typescript
1. Lazy Loading للمكونات الكبيرة
   ✅ موجود بالفعل في CatchAllPageClient.tsx

2. Caching للمواقيت
   ✅ موجود في prayerNotifications.ts

3. Debounce لحفظ الحالة
   ✅ موجود في useEditor.tsx

4. Service Worker للـ Offline
   ✅ موجود في public/sw.js
```

### **لتحسين البطارية:**
```typescript
1. تقليل Polling
   ✅ الإشعارات تُجدَّد كل 30 دقيقة فقط

2. استخدام LocalNotifications بدلاً من Push
   ✅ للأذان (لا يحتاج إنترنت)

3. إيقاف التحديثات في الخلفية
   ✅ عند إغلاق التطبيق
```

---

## 📞 15. الدعم الفني

### **إذا واجهت مشكلة:**
```bash
1. شغّل Logcat وابحث عن الأخطاء:
   adb logcat > logs.txt

2. راجع Firebase Console للأخطاء

3. تحقق من Vercel Logs للـ API

4. اختبر على جهاز حقيقي (ليس Emulator)
```

---

## ✅ الخلاصة

### **كل شيء يعمل إذا:**
```
✅ الصلاحيات مُعطاة (إشعارات + موقع)
✅ تحسين البطارية مُعطّل للتطبيق
✅ Firebase Config صحيح
✅ API Keys موجودة (.env.local)
✅ اتصال إنترنت متاح (لل一些 الميزات)
```

### **الميزات التي تعمل بدون إنترنت:**
```
✅ قراءة المصحف (بعد التحميل)
✅ الأذكار
✅ عداد النقاط (محلي)
✅ الأذان (بعد الجدولة)
✅ القبلة (بعد تحديد الموقع)
```

### **الميزات التي تحتاج إنترنت:**
```
✅ تسجيل الدخول
✅ مواقيت الصلاة (أول مرة فقط)
✅ المكتبة الصوتية
✅ صناعة الفيديو
✅ ChatBot
✅ لوحة المتصدرين
```

---

**🎉 تهانينا! تطبيقك جاهز للاستخدام!**

لأي استفسارات، راجع:
- README.md
- PROJECT_GUIDE_V3.md
- NOTIFICATION_SYSTEM_MASTER_PLAN.md
