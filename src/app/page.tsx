import CatchAllPage, { dynamic as dynamicConfig, dynamicParams as paramsConfig } from "./[[...slug]]/page";

export const dynamic = dynamicConfig;
export const dynamicParams = paramsConfig;

export default CatchAllPage;
